version: '3.8'

services:
  ticket-router:
    build: .
    container_name: ticket-router
    volumes:
      # Bind mount the data directory so generated processed_tickets.json is accessible on host
      - ./data:/app/data
